name: Interop

# Lightweight interop suite that gates wire-protocol, policy, and
# EVPN behavior against a real FRR peer. Each job is independent and
# runs in parallel; ~3 min wall-clock per job dominated by the FRR
# image pull and the rustbgpd:dev docker build (cached).
#
# Foundation tier — wire-protocol + core RIB / refresh / policy:
#   - M1  (~10 s test): basic session + UPDATE + Adj-RIB-In + gRPC
#     ListReceivedRoutes + withdrawal. If this regresses, every
#     other interop test below is meaningless.
#   - M13 (~10 s test): policy engine end-to-end (import + export
#     chains; set_local_pref / set_med / set_community_add /
#     AS_PATH regex / prefix deny). 3-node topology (rustbgpd
#     between FRR-A and FRR-B).
#   - M15 (~10 s test): Route Refresh (RFC 2918 + RFC 7313) via
#     gRPC SoftResetIn. Companion to M34 — together they pin both
#     ends of the contract (gRPC-driven and SIGHUP-driven refresh).
#   - M80 (~60 s test): rpol policy parity (ADR-0096) — rustbgpd
#     runs its policy from an `.rpol` file while an FRR parity node
#     expresses the same intent as route-maps / prefix-lists /
#     community-lists; asserts route-for-route import + export
#     agreement (prefix-set ge/le, community add/remove, LP, as-path
#     regex reject, modify-then-continue, a parameterized policy
#     instantiated twice), `rbgp policy check` / `policy test` /
#     `policy stats`, and an .rpol edit + SIGHUP hot-apply whose
#     Route Refresh is scoped to the one peer whose chain changed.
#
# Address-family + topology tier — MP-BGP, RR, multi-path:
#   - M10 (~10 s test): IPv6 dual-stack via MP_REACH_NLRI /
#     MP_UNREACH_NLRI, IPv4 + IPv6 routes over the same session.
#     Wire-level fragility check for MP-BGP.
#   - M14 (~10 s test): Route Reflector (RFC 4456) — RR + 2 iBGP
#     clients, validates CLUSTER_LIST + ORIGINATOR_ID set
#     correctly on reflected routes. Spine of every IX deployment.
#   - M17 (~10 s test): Add-Path (RFC 7911) — multi-path send with
#     distinct path_ids over a 4-node topology (rustbgpd between
#     two upstreams + one client). Wire-level path_id encoding +
#     capability-negotiation fragility.
#   - M89 (~15 s test): experimental Paths-Limit capability 76 —
#     three real FRR sources feed dual-stack candidate sets to a
#     real FRR sink whose unequal receive limits cap Add-Path export.
#   - M73 (~10 s test): BGP-LS reflection (RFC 9552) — GoBGP v4
#     source injects a Node NLRI, rustbgpd reflects it as RR, and
#     GoBGP v4 sink validates receipt plus withdrawal.
#   - M74 (~15 s test): VPNv4 route reflection (RFC 4364, ADR-0077
#     §9 gate) — GoBGP source injects RD+label+RT routes, rustbgpd
#     reflects with next-hop/label/RD/RT preserved verbatim, sink
#     validates receipt, withdrawal, and no dataplane install.
#   - M75 (~30 s test): RT-Constrain (RFC 4684) VPNv4 reflection
#     filtering — GoBGP source with two VRFs, an RTC sink that must
#     receive only its member RT, a non-RTC sink that must receive
#     everything, plus widen/narrow membership churn with zero
#     session flaps and no UPDATE storm.
#   - M76 (~60 s test): Optimal Route Reflection (RFC 9107) — GoBGP
#     v4 source injects a 4-link BGP-LS square, two PEs announce the
#     same prefix with different next-hops, and two clients pinned
#     to different ORR vantages must receive DIVERGENT best paths,
#     flip on a topology metric change (no churn to the unaffected
#     client), and fall back to one standard best on withdrawal.
#   - M77 (~4 min test): GR/LLGR stale preservation for the RR
#     families (RFC 4724 / RFC 9494) — a GoBGP PE (VPNv4 + RTC) is
#     kill -9'd: rustbgpd must hold its routes stale, keep serving
#     the client, sweep exactly the not-re-advertised route at the
#     relaunched (`gobgpd -r`) PE's End-of-RIB, promote to the LLGR
#     stale phase (LLGR_STALE toward the client) when the PE never
#     returns, and keep the ORR vantage resolved through a BGP-LS
#     source's GR window.
#   - M78 (~60 s test): multi-cluster ORR — two rustbgpd RRs with
#     divergent vantages exchange Add-Path over the inter-RR mesh
#     so per-cluster optimal routes survive the second reflection
#     hop.
#   - M79 (~90 s test): labeled-unicast route reflection (RFC 8277,
#     SAFI 4) — GoBGP PE injects IPv4 + IPv6 labeled routes
#     (including a multi-label stack), rustbgpd reflects with label
#     stack/next-hop preserved verbatim + ORIGINATOR_ID/CLUSTER_LIST,
#     relabel is an implicit replace, withdrawal uses the §2.4
#     compatibility field, a kill -9 GR window preserves the routes
#     stale with zero client-facing UPDATEs, and the relaunched
#     PE's End-of-RIB sweeps exactly the not-re-advertised route.
#
# Operational + security tier — BMP monitoring, transport security,
# FlowSpec:
#   - M22 (~30 s test): FlowSpec (RFC 5575/8955) injection,
#     distribution, and withdrawal. Wait windows are deliberately
#     wide (60 s) because FRR's `show bgp ipv4 flowspec` view
#     reconverges slowly after MP_UNREACH — observed up to ~44 s
#     of empty display before rule 2 reappears. Underlying
#     data-plane state is correct; the lag is FRR's display path.
#   - M24 (~10 s test): BMP (RFC 7854) — Initiation, PeerUp, and
#     RouteMonitoring messages received by a BMP receiver. Adds
#     a python:3.12-slim container running scripts/bmp-receiver.py.
#     Catches RibManager BMP integration regressions.
#   - M81 (~3 min test): the BMP trio receipt — RFC 8671 Adj-RIB-Out
#     + RFC 9069 Loc-RIB (collector-connect dump + EoR) + BMPv4 TLV
#     framing (draft-ietf-grow-bmp-tlv-20) + Path Marking
#     (path-marking-tlv-05) against three collectors at once:
#     pmacct pmbmpd bleeding-edge + gobmp (semantic v3 oracles,
#     digest-pinned) and a raw byte sink with tshark 4.4 pcap
#     dissection for the v4 frames. 50 assertions; the wall time is
#     dominated by the 60 s stats cadence.
#   - M25 (~45 s test): TCP MD5 (RFC 2385) + GTSM / TTL security
#     (RFC 5082). Two static FRR peers cover MD5 and GTSM separately;
#     a third FRR peer active-opens over IPv6 into a dynamic range
#     requiring both, including wrong-MD5 and missing-GTSM negative
#     phases. Catches active- and accepted-socket transport regressions.
#
# EVPN + SIGHUP tier:
#   - M29 (~10 s test): control-plane sanity — session Established
#     + L2VPN/EVPN capability negotiation + gRPC ListEvpnRoutes
#     shape. No kernel VXLAN, no MAC learning.
#   - M30 (~6 s test): EVPN Type 2 MAC reflection end-to-end with
#     kernel VXLAN + bridge per VTEP. Empirically viable on
#     ubuntu-latest (kernel 6.17.0-101x-azure ships vxlan + bridge;
#     vrf is missing, which keeps M30b out of CI).
#   - M34 (~15 s test): SIGHUP-driven policy edit fires an automatic
#     Route Refresh and re-imports against the new policy. Guards
#     the auto-fire path inside `PeerManager::update_runtime_policies`
#     introduced post-v0.11.0. Plain BGP + IPv4 unicast.
#   - M35/M35b/M35c (~30 s each): RFC 8326 Graceful Shutdown receiver
#     and initiator behavior. M35 covers IPv4 unicast, M35b FlowSpec,
#     M35c L2VPN/EVPN so all outbound attach sites are exercised
#     against FRR.
#   - M41 (~15 s test): RFC 7999 BLACKHOLE (65535:666) — FRR tags a
#     /32 with BLACKHOLE, rustbgpd's `honor_blackhole = true` chain-
#     tail rule preserves it and adds NO_ADVERTISE while an untagged
#     /32 stays untouched. Plain BGP + IPv4 unicast.
#
# M30b/M31/M32/M33 stay manual: M30b needs the missing `vrf` module
# for L3VNI binding (RFC 9136 Type 5); M32 needs a bond ES; M33 needs
# wall time. See "CI wiring for M29-M33" in ROADMAP.md.

on:
  push:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "docs/**"
  pull_request:
    paths-ignore:
      - "**/*.md"
      - "docs/**"

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  # Warm one fixed-scope cache per source SHA; jobs still build and load their
  # own image, falling back to an uncached build if the cache is unavailable.
  prime_dev_image:
    name: Prime rustbgpd:dev build cache
    runs-on: ubuntu-latest
    timeout-minutes: 30
    concurrency:
      group: rustbgpd-dev-image-${{ github.sha }}
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.sha }}
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Prime rustbgpd:dev build cache
        uses: docker/build-push-action@v7
        with:
          context: .
          load: false
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev
          cache-to: type=gha,scope=rustbgpd-dev,mode=max,ignore-error=true

  m1:
    needs: prime_dev_image
    name: M1 — basic session + UPDATE (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M1 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M1
          topology: tests/interop/m1-frr.clab.yml
          script: tests/interop/scripts/test-m1-frr.sh

  m13:
    needs: prime_dev_image
    name: M13 — Policy Engine (FRR 10.3.1, 3-node)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M13 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M13
          topology: tests/interop/m13-policy-frr.clab.yml
          script: tests/interop/scripts/test-m13-policy-frr.sh

  m80:
    needs: prime_dev_image
    name: M80 — rpol policy parity (ADR-0096, FRR 10.3.1, 4-node)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq drives the parity tuple comparisons (rustbgpd gRPC JSON
        # vs FRR vtysh JSON) and the policy test/stats assertions.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M80 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M80
          topology: tests/interop/m80-rpol-policy-parity-frr.clab.yml
          script: tests/interop/scripts/test-m80-rpol-policy-parity-frr.sh

  m15:
    needs: prime_dev_image
    name: M15 — Route Refresh via gRPC SoftResetIn (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M15 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M15
          topology: tests/interop/m15-rr-frr.clab.yml
          script: tests/interop/scripts/test-m15-rr-frr.sh

  m10:
    needs: prime_dev_image
    name: M10 — IPv6 dual-stack / MP-BGP (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M10 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M10
          topology: tests/interop/m10-frr-ipv6.clab.yml
          script: tests/interop/scripts/test-m10-frr-ipv6.sh

  m14:
    needs: prime_dev_image
    name: M14 — Route Reflector (FRR 10.3.1, 3-node iBGP)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M14 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M14
          topology: tests/interop/m14-rr-frr.clab.yml
          script: tests/interop/scripts/test-m14-rr-frr.sh

  m17:
    needs: prime_dev_image
    name: M17/M89 — Add-Path + experimental Paths-Limit (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M17 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M17
          topology: tests/interop/m17-addpath-frr.clab.yml
          script: tests/interop/scripts/test-m17-addpath-frr.sh

      - name: Run M89 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M89
          topology: tests/interop/m89-paths-limit-frr.clab.yml
          script: tests/interop/scripts/test-m89-paths-limit-frr.sh

  m73:
    needs: prime_dev_image
    name: M73/M87 — GoBGP BGP-LS + exact-export/BIRD
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:bgpls
        run: docker build -t gobgp:bgpls -f tests/interop/Dockerfile.gobgp-bgpls tests/interop

      - name: Build bird:2-bookworm
        run: docker build -t bird:2-bookworm -f tests/interop/Dockerfile.bird tests/interop

      - name: Run M73 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M73
          topology: tests/interop/m73-bgpls-reflection-gobgp.clab.yml
          script: tests/interop/scripts/test-m73-bgpls-reflection-gobgp.sh

      - name: Run M87 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M87
          topology: tests/interop/m87-exact-export-rejection.clab.yml
          script: tests/interop/scripts/test-m87-exact-export-rejection.sh

  m74:
    needs: prime_dev_image
    name: M74 — VPNv4 route reflection (GoBGP 3.x)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:interop
        run: docker build -t gobgp:interop -f tests/interop/Dockerfile.gobgp tests/interop

      - name: Run M74 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M74
          topology: tests/interop/m74-vpnv4-reflection-gobgp.clab.yml
          script: tests/interop/scripts/test-m74-vpnv4-reflection-gobgp.sh

  m75:
    needs: prime_dev_image
    name: M75 — RT-Constrain VPNv4 filtering (GoBGP 3.x)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:interop
        run: docker build -t gobgp:interop -f tests/interop/Dockerfile.gobgp tests/interop

      - name: Run M75 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M75
          topology: tests/interop/m75-rtc-vpnv4-filter-gobgp.clab.yml
          script: tests/interop/scripts/test-m75-rtc-vpnv4-filter-gobgp.sh

  m76:
    needs: prime_dev_image
    name: M76 — ORR divergent-best (GoBGP 4.6.0)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:bgpls
        run: docker build -t gobgp:bgpls -f tests/interop/Dockerfile.gobgp-bgpls tests/interop

      - name: Run M76 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M76
          topology: tests/interop/m76-orr-divergent-best-gobgp.clab.yml
          script: tests/interop/scripts/test-m76-orr-divergent-best-gobgp.sh

  m77:
    needs: prime_dev_image
    name: M77 — GR/LLGR RR-family stale preservation (GoBGP 4.6.0)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:bgpls
        run: docker build -t gobgp:bgpls -f tests/interop/Dockerfile.gobgp-bgpls tests/interop

      - name: Run M77 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M77
          topology: tests/interop/m77-gr-llgr-rr-gobgp.clab.yml
          script: tests/interop/scripts/test-m77-gr-llgr-rr-gobgp.sh

  m78:
    needs: prime_dev_image
    name: M78 — Multi-cluster ORR + inter-RR Add-Path (GoBGP 4.6.0)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:bgpls
        run: docker build -t gobgp:bgpls -f tests/interop/Dockerfile.gobgp-bgpls tests/interop

      - name: Run M78 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M78
          topology: tests/interop/m78-multicluster-orr-gobgp.clab.yml
          script: tests/interop/scripts/test-m78-multicluster-orr-gobgp.sh

  m79:
    needs: prime_dev_image
    name: M79 — Labeled-unicast route reflection + GR (GoBGP 4.6.0)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:bgpls
        run: docker build -t gobgp:bgpls -f tests/interop/Dockerfile.gobgp-bgpls tests/interop

      - name: Run M79 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M79
          topology: tests/interop/m79-labeled-reflection-gobgp.clab.yml
          script: tests/interop/scripts/test-m79-labeled-reflection-gobgp.sh

  m22:
    needs: prime_dev_image
    name: M22 — FlowSpec (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq parses FRR's `show bgp ipv4 flowspec json` output for
        # the direct convergence checks and `show bgp summary json`
        # for the per-step flap-detection assertion.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M22 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M22
          topology: tests/interop/m22-flowspec-frr.clab.yml
          script: tests/interop/scripts/test-m22-flowspec-frr.sh

  m24:
    needs: prime_dev_image
    name: M24 — BMP (FRR 10.3.1, Python receiver)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M24 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M24
          topology: tests/interop/m24-bmp-frr.clab.yml
          script: tests/interop/scripts/test-m24-bmp-frr.sh

  m81:
    needs: prime_dev_image
    name: M81 — BMP trio + BMPv4 receipt (pmacct + gobmp + tshark)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq drives the pmacct-msglog and gobmp-JSON oracle assertions.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:interop
        run: docker build -t gobgp:interop -f tests/interop/Dockerfile.gobgp tests/interop

      - name: Build bmpsink:m81
        # tshark 4.4 (alpine 3.22) + python3: raw BMP byte sink and the
        # pcap dissection oracle. The pmacct/gobmp collector images are
        # digest-pinned in the topology file and pulled at deploy.
        run: docker build -t bmpsink:m81 -f tests/interop/Dockerfile.bmpsink tests/interop

      - name: Run M81 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M81
          topology: tests/interop/m81-bmp-trio-gobgp.clab.yml
          script: tests/interop/scripts/test-m81-bmp-trio-gobgp.sh

  m82:
    needs: prime_dev_image
    # Synthetic leg only. The SR Linux vendor leg
    # (m82-evpn-bundle-srlinux.clab.yml) stays local: the NOS image is
    # ~750 MB compressed / 3.2 GB on disk and boots a full chassis sim —
    # outside the per-job budget for a receipt the synthetic leg
    # already gates.
    name: M82 — EVPN VLAN-aware-bundle reflection, synthetic leg (GoBGP 3.x)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build gobgp:interop
        run: docker build -t gobgp:interop -f tests/interop/Dockerfile.gobgp tests/interop

      - name: Run M82 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M82
          topology: tests/interop/m82-evpn-bundle-synthetic-gobgp.clab.yml
          script: tests/interop/scripts/test-m82-evpn-bundle-synthetic-gobgp.sh

  m83:
    needs: prime_dev_image
    # Full 3-stack leg in CI: BIRD 2 is a plain apt install on
    # bookworm-slim (deterministic build) and BIRD already runs in
    # hosted CI (M43, kernel-dataplane.yml) without flake history —
    # no BIRD-manual fallback needed.
    name: M83 — route-server profile, multi-stack (BIRD 2 + GoBGP + FRR + RTR)
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq drives the FRR vtysh / GoBGP JSON client-view assertions.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build bird:2-bookworm
        # bird2 + tshark: the member stack that carries the byte-level
        # RFC 7947 transparency / OTC / EoR wire assertions.
        run: docker build -t bird:2-bookworm -f tests/interop/Dockerfile.bird tests/interop

      - name: Build gobgp:interop
        run: docker build -t gobgp:interop -f tests/interop/Dockerfile.gobgp tests/interop

      - name: Run M83 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M83
          topology: tests/interop/m83-routeserver-multistack.clab.yml
          script: tests/interop/scripts/test-m83-routeserver-multistack.sh

      - name: Upload M83 failed-attempt evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: m83-failed-attempts-${{ github.run_attempt }}
          path: ${{ runner.temp }}/m83-failure-artifacts
          if-no-files-found: ignore
          retention-days: 14

  m85:
    needs: prime_dev_image
    name: M85/M93/M95 — RR core, required families, and RFC 8212 presence transitions against BIRD 2
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq drives the rbgp JSON client-view assertions.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Build bird:2-bookworm
        run: docker build -t bird:2-bookworm -f tests/interop/Dockerfile.bird tests/interop

      - name: Run M85 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M85
          topology: tests/interop/m85-rr-bird.clab.yml
          script: tests/interop/scripts/test-m85-rr-bird.sh

      - name: Run M93 (required-family OPEN enforcement)
        uses: ./.github/actions/run-interop-test
        with:
          label: M93
          topology: tests/interop/m93-required-families-bird.clab.yml
          script: tests/interop/scripts/test-m93-required-families-bird.sh

      # M95 also needs FRR, which this runner already has via the other jobs'
      # images; the topology pulls it. BIRD is the load-bearing half: it is the
      # only speaker in the matrix that can be told not to advertise Route
      # Refresh, which is what ADR-0112's rejection path requires.
      - name: Run M95 (RFC 8212 live policy-presence transitions)
        uses: ./.github/actions/run-interop-test
        with:
          label: M95
          topology: tests/interop/m95-rfc8212-presence-frr-bird.clab.yml
          script: tests/interop/scripts/test-m95-rfc8212-presence.sh

  m94:
    needs: prime_dev_image
    name: M94 — RFC 6793 AS4 migration against OLD speakers
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq drives the exact reconstructed-RIB and wire-receipt assertions.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Pull pinned ExaBGP 5.0.9 source
        run: docker pull ghcr.io/exa-networks/exabgp@sha256:5554ac053766e66ca3b99eb6b66e83e32113913729a184dcbaf0cc4abd2f4244

      - name: Run M94 (AS4 migration wire oracle)
        # Load-bearing: the two independent OLD-speaker witnesses make this red
        # if AS4_PATH reconstruction/projection or AS4_AGGREGATOR is removed.
        uses: ./.github/actions/run-interop-test
        with:
          label: M94
          topology: tests/interop/m94-as4-migration-exabgp.clab.yml
          script: tests/interop/scripts/test-m94-as4-migration-exabgp.sh

  m86:
    needs: prime_dev_image
    name: M86 — RR core reflection + GR truth against OpenBGPD 9.1 clients
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq drives the bgpctl -j and rbgp JSON client-view assertions.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Pull openbgpd/openbgpd:9.1
        # Pinned upstream image from the OpenBGPD portable project; its
        # entrypoint execs any non-bgpd command, so the topology holds
        # the container with `sleep infinity` and the driver launches
        # bgpd itself.
        run: docker pull openbgpd/openbgpd:9.1

      - name: Run M86 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M86
          topology: tests/interop/m86-rr-openbgpd.clab.yml
          script: tests/interop/scripts/test-m86-rr-openbgpd.sh

  m25:
    needs: prime_dev_image
    name: M25 — TCP MD5 + GTSM (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M25 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M25
          topology: tests/interop/m25-md5-gtsm-frr.clab.yml
          script: tests/interop/scripts/test-m25-md5-gtsm-frr.sh

  m29:
    needs: prime_dev_image
    name: M29 — EVPN cap negotiation (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      # containerlab and grpcurl are installed locally to the runner;
      # both are cheap (single Go binaries). Pinned versions to keep the
      # gate reproducible — bumping is a deliberate change.
      - name: Install containerlab
        # Version is pinned in the install-containerlab composite (default
        # input). Bumping is a deliberate change; verify a clean M29 run on the
        # new version before merging.
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      # Buildx cache cuts the docker build step from ~6 min to ~30 s on
      # cache hits. The cargo registry / target dir gets reused via the
      # GitHub Actions cache backend (type=gha).
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M29 (deploy + test + destroy with retry)
        # The composite always destroys before each attempt, so an
        # orphan container from a prior failed run cannot leak in.
        uses: ./.github/actions/run-interop-test
        with:
          label: M29
          topology: tests/interop/m29-evpn-rr-frr.clab.yml
          script: tests/interop/scripts/test-m29-evpn-rr-frr.sh

  m30:
    needs: prime_dev_image
    name: M30 — EVPN Type 2 reflection (FRR 10.3.1, kernel VXLAN)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq is required for M30 test 6's strict next_hop assertion
        # (scoped to the entry containing the test MAC); a substring
        # grep would let a malformed match slip past.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M30 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M30
          topology: tests/interop/m30-evpn-type2-frr.clab.yml
          script: tests/interop/scripts/test-m30-evpn-type2-frr.sh

  m34:
    needs: prime_dev_image
    name: M34 — SIGHUP policy soft-reset (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq is required for the strict per-prefix counting + session
        # uptime epoch comparison in test-m34.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M34 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M34
          topology: tests/interop/m34-policy-soft-reset-frr.clab.yml
          script: tests/interop/scripts/test-m34-policy-soft-reset-frr.sh

  m35:
    needs: prime_dev_image
    name: M35 — RFC 8326 Graceful Shutdown (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq parses NeighborState.localPref + FRR's `show ip bgp` JSON
        # for the community-presence assertion.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M35 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M35
          topology: tests/interop/m35-graceful-shutdown-frr.clab.yml
          script: tests/interop/scripts/test-m35-graceful-shutdown-frr.sh

  m35b:
    needs: prime_dev_image
    name: M35b — RFC 8326 GShut FlowSpec (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq parses FRR's `show bgp ipv4 flowspec json` output for
        # the community-presence assertion.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M35b (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M35b
          topology: tests/interop/m35b-graceful-shutdown-flowspec-frr.clab.yml
          script: tests/interop/scripts/test-m35b-graceful-shutdown-flowspec-frr.sh

  m35c:
    needs: prime_dev_image
    name: M35c — RFC 8326 GShut EVPN (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq pretty-prints EVPN diagnostics and parses grpcurl JSON.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M35c (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M35c
          topology: tests/interop/m35c-graceful-shutdown-evpn-frr.clab.yml
          script: tests/interop/scripts/test-m35c-graceful-shutdown-evpn-frr.sh

  m41:
    needs: prime_dev_image
    name: M41 — RFC 7999 BLACKHOLE (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq parses `ListReceivedRoutes` JSON for the per-prefix
        # community assertions (BLACKHOLE + implicit NO_ADVERTISE
        # on tagged /32, neither on untagged /32).
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M41 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M41
          topology: tests/interop/m41-blackhole-frr.clab.yml
          script: tests/interop/scripts/test-m41-blackhole-frr.sh

  m44:
    needs: prime_dev_image
    name: M44 — ADR-0064 gRPC tier authorization (mTLS)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      # The mTLS PKI fixture must exist before deploy so the topology
      # can bind-mount it into the rustbgpd container. Generated here
      # (not in the test script) because the retry composite deploys
      # before running the script.
      - name: Generate M44 mTLS certs
        run: bash tests/interop/scripts/gen-m44-certs.sh

      - name: Run M44 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M44
          topology: tests/interop/m44-grpc-tier-authz.clab.yml
          script: tests/interop/scripts/test-m44-grpc-tier-authz.sh

  m54:
    needs: prime_dev_image
    name: M54 — ADR-0070 gNMI / OpenConfig over mTLS
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl and gnmic
        run: |
          bash .github/scripts/install-grpcurl.sh
          curl -sL https://github.com/openconfig/gnmic/releases/download/v0.46.0/gnmic_0.46.0_Linux_x86_64.tar.gz \
            | sudo tar -xz -C /usr/local/bin gnmic
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      # The mTLS PKI fixture must exist before deploy so the topology
      # can bind-mount it into the rustbgpd container.
      - name: Generate M54 mTLS certs
        run: bash tests/interop/scripts/gen-m54-certs.sh

      - name: Run M54 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M54
          topology: tests/interop/m54-gnmi-openconfig.clab.yml
          script: tests/interop/scripts/test-m54-gnmi-openconfig.sh

  m55:
    needs: prime_dev_image
    name: M55 — ADR-0071 BGP Roles + OTC (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl and jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M55 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M55
          topology: tests/interop/m55-bgp-roles-otc-frr.clab.yml
          script: tests/interop/scripts/test-m55-bgp-roles-otc-frr.sh

  m56:
    needs: prime_dev_image
    name: M56 — gNMI Subscribe ON_CHANGE (ADR-0070 deferral resolved)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl, gnmic, and jq
        run: |
          bash .github/scripts/install-grpcurl.sh
          curl -sL https://github.com/openconfig/gnmic/releases/download/v0.46.0/gnmic_0.46.0_Linux_x86_64.tar.gz \
            | sudo tar -xz -C /usr/local/bin gnmic
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      # Reuses the M54 mTLS PKI; same operator cert chain.
      - name: Generate M54 mTLS certs
        run: bash tests/interop/scripts/gen-m54-certs.sh

      - name: Run M56 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M56
          topology: tests/interop/m56-gnmi-onchange-frr.clab.yml
          script: tests/interop/scripts/test-m56-gnmi-onchange-frr.sh

  m45:
    needs: prime_dev_image
    name: M45 — EVPN Type 5 injection (gRPC → FRR)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl
        run: |
          bash .github/scripts/install-grpcurl.sh

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M45 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M45
          topology: tests/interop/m45-evpn-type5-injection.clab.yml
          script: tests/interop/scripts/test-m45-evpn-type5-injection.sh

  m57:
    needs: prime_dev_image
    name: M57 — Outbound Route Filtering (RFC 5291/5292, FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # grpcurl injects routes via InjectionService.AddPath; jq parses FRR's
        # `show bgp ipv4 unicast json` for the received-prefix-set assertions.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M57 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M57
          topology: tests/interop/m57-orf-frr.clab.yml
          script: tests/interop/scripts/test-m57-orf-frr.sh

  m63:
    needs: prime_dev_image
    name: M63 — ADR-0078 stalled-RIB hold-timer survival (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq parses GetNeighborState / ListReceivedRoutes JSON for the
        # survival-window and never-drop assertions; the Prometheus
        # counters come over plain HTTP.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M63 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M63
          topology: tests/interop/m63-stalled-rib-hold-timer.clab.yml
          script: tests/interop/scripts/test-m63-stalled-rib-hold-timer.sh

  m64:
    needs: prime_dev_image
    name: M64 — IPv6-only peering via disable_ipv4_unicast (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - name: Install containerlab
        uses: ./.github/actions/install-containerlab

      - name: Install grpcurl + jq
        # jq parses FRR's `show bgp neighbors json` capability output for
        # the no-IPv4-MP assertion; grpcurl injects the bidirectional
        # route-exchange prefixes and reads rustbgpd's RIB.
        run: |
          bash .github/scripts/install-grpcurl.sh
          command -v jq >/dev/null || { sudo apt-get update -y && sudo apt-get install -y jq; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build rustbgpd:dev
        uses: docker/build-push-action@v7
        with:
          context: .
          load: true
          tags: rustbgpd:dev
          target: dev
          cache-from: type=gha,scope=rustbgpd-dev

      - name: Run M64 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M64
          topology: tests/interop/m64-ipv6-only.clab.yml
          script: tests/interop/scripts/test-m64-ipv6-only.sh
