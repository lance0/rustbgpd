name: Kernel Dataplane

# Privileged Linux kernel dataplane interop suite. Runs on GitHub-hosted
# `ubuntu-latest`: a spike (2026-05-25) confirmed the hosted Azure kernel + a
# `docker run --cap-add` / passwordless-sudo VM provides everything these tests
# need — bridge + VXLAN FDB, FDB nexthop groups (kernel >= 5.8), real netns
# route tables, CONFIG_TCP_AO, and UDP/3784 BFD. The one gap, the `vrf` module
# (L3VNI / symmetric IRB), is supplied by `linux-modules-extra-<kver>`; the
# `setup-dataplane-host` action loads it. This retired the self-hosted runner.
#
# Each hosted job is an isolated VM, so it runs `setup-dataplane-host` to install
# containerlab (hardened, issue #208) + grpcurl + jq + kernel modules and build
# its own `rustbgpd:dev` (GHA layer cache) — there is no shared persistent daemon
# to fan out from, and stale-topology accumulation (issue #188) can't happen.

on:
  push:
    branches: [main]
    paths-ignore:
      - "**/*.md"
      - "docs/**"
  pull_request:
    paths-ignore:
      - "**/*.md"
      - "docs/**"
  schedule:
    # 07:00 UTC nightly — catches dataplane regressions even if a PR was
    # never re-run.
    - cron: '0 7 * * *'
  workflow_dispatch:

concurrency:
  # Cancel superseded runs of this workflow for the same ref; hosted jobs are
  # isolated VMs so there is no shared-daemon reason to serialize anymore.
  group: kernel-dataplane-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  m36:
    name: M36 — EVPN VTEP receive FDB programming (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M36 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M36
          topology: tests/interop/m36-evpn-vtep-smoke.clab.yml
          script: tests/interop/scripts/test-m36-evpn-vtep-smoke.sh

  m37:
    name: M37 — EVPN local MAC origination (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M37 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M37
          topology: tests/interop/m37-evpn-local-origination.clab.yml
          script: tests/interop/scripts/test-m37-evpn-local-origination.sh

  m37-ip:
    name: M37+IP — EVPN local MAC/IP origination (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M37+IP (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M37+IP
          topology: tests/interop/m37-evpn-mac-ip-origination.clab.yml
          script: tests/interop/scripts/test-m37-evpn-mac-ip-origination.sh

  m38:
    name: M38 — EVPN DF election + Type 1/4 origination
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M38 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M38
          topology: tests/interop/m38-evpn-df-election.clab.yml
          script: tests/interop/scripts/test-m38-evpn-df-election.sh

  m39:
    name: M39 — EVPN Type 5 symmetric Interface-less IRB (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M39 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M39
          topology: tests/interop/m39-evpn-type5-symmetric-irb.clab.yml
          script: tests/interop/scripts/test-m39-evpn-type5-symmetric-irb.sh

  m39b:
    name: M39b — EVPN auto-derived Route Targets cross-vendor (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M39b (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M39b
          topology: tests/interop/m39b-evpn-auto-rt-frr.clab.yml
          script: tests/interop/scripts/test-m39b-evpn-auto-rt-frr.sh

  m48:
    name: M48 — EVPN runtime tenant teardown over kernel L3 datapath (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M48 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M48
          topology: tests/interop/m48-evpn-tenant-teardown-datapath.clab.yml
          script: tests/interop/scripts/test-m48-evpn-tenant-teardown-datapath.sh

  m60:
    name: M60 — ADR-0079 EVPN adoption sweep kill-and-restart (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M60 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M60
          topology: tests/interop/m60-evpn-adoption-sweep.clab.yml
          script: tests/interop/scripts/test-m60-evpn-adoption-sweep.sh

  m61:
    name: M61 — ADR-0079 EVPN L3 adoption sweep kill-and-restart (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M61 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M61
          topology: tests/interop/m61-evpn-l3-adoption-sweep.clab.yml
          script: tests/interop/scripts/test-m61-evpn-l3-adoption-sweep.sh

  m62:
    name: M62 — ADR-0079 blackhole adoption sweep kill-and-restart (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M62 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M62
          topology: tests/interop/m62-blackhole-adoption-sweep.clab.yml
          script: tests/interop/scripts/test-m62-blackhole-adoption-sweep.sh

  m40:
    name: M40 — ADR-0059 aliasing dataplane ECMP (FRR EVPN-MH 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M40 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M40
          topology: tests/interop/m40-evpn-aliasing-ecmp-frr.clab.yml
          script: tests/interop/scripts/test-m40-evpn-aliasing-ecmp-frr.sh

  m42:
    name: M42 — ADR-0061 configured-table unicast FIB runtime (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M42 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M42
          topology: tests/interop/m42-fib-runtime-frr.clab.yml
          script: tests/interop/scripts/test-m42-fib-runtime-frr.sh

  m50:
    name: M50 — ADR-0066 unicast multipath/ECMP FIB install (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M50 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M50
          topology: tests/interop/m50-fib-ecmp-frr.clab.yml
          script: tests/interop/scripts/test-m50-fib-ecmp-frr.sh

  m52:
    name: M52 — ADR-0066 multipath-relax (FRR 10.3.1, mixed ASes)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M52 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M52
          topology: tests/interop/m52-fib-ecmp-relax-frr.clab.yml
          script: tests/interop/scripts/test-m52-fib-ecmp-relax-frr.sh

  m58:
    name: M58 — ADR-0061 FIB-table runtime CRUD (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M58 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M58
          topology: tests/interop/m58-fib-table-crud-frr.clab.yml
          script: tests/interop/scripts/test-m58-fib-table-crud-frr.sh

  m53:
    name: M53 — ADR-0069 BGP unnumbered + scoped FIB (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M53 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M53
          topology: tests/interop/m53-bgp-unnumbered-frr.clab.yml
          script: tests/interop/scripts/test-m53-bgp-unnumbered-frr.sh

  m51:
    name: M51 — ADR-0067 single-hop BFD + RFC 5882 coupling (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M51 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M51
          topology: tests/interop/m51-bfd-frr.clab.yml
          script: tests/interop/scripts/test-m51-bfd-frr.sh

  m43:
    name: M43 — ADR-0062 TCP-AO protected session (BIRD 3.2.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host

      - name: Detect TCP-AO kernel support
        id: tcp_ao
        run: |
          set -euo pipefail
          supported=unknown
          if [ -f "/boot/config-$(uname -r)" ]; then
            if grep -q '^CONFIG_TCP_AO=y' "/boot/config-$(uname -r)"; then
              supported=true
            else
              supported=false
            fi
          elif [ -f /proc/config.gz ]; then
            if zgrep -q '^CONFIG_TCP_AO=y' /proc/config.gz; then
              supported=true
            else
              supported=false
            fi
          else
            echo "::warning::Kernel config not readable; M43 will rely on runtime TCP-AO probe"
            supported=true
          fi

          echo "supported=$supported" >> "$GITHUB_OUTPUT"
          if [ "$supported" = "false" ]; then
            echo "::warning::M43 skipped: $(uname -r) does not advertise CONFIG_TCP_AO=y"
          fi

      - name: Build BIRD 3.2.1 TCP-AO image
        if: steps.tcp_ao.outputs.supported == 'true'
        run: docker build -t bird:3.2.1-tcpao -f tests/interop/Dockerfile.bird3 tests/interop

      - name: Run M43 (deploy + test + destroy with retry)
        if: steps.tcp_ao.outputs.supported == 'true'
        uses: ./.github/actions/run-interop-test
        with:
          label: M43
          topology: tests/interop/m43-tcp-ao-bird.clab.yml
          script: tests/interop/scripts/test-m43-tcp-ao-bird.sh

  m46:
    name: M46 — EVPN HRW DF election (rustbgpd ×2)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M46 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M46
          topology: tests/interop/m46-evpn-df-hrw.clab.yml
          script: tests/interop/scripts/test-m46-evpn-df-hrw.sh

  m47:
    name: M47 — EVPN runtime tenant teardown (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M47 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M47
          topology: tests/interop/m47-evpn-tenant-teardown.clab.yml
          script: tests/interop/scripts/test-m47-evpn-tenant-teardown.sh

  m49:
    name: M49 — EVPN preference-DF election (rustbgpd ×2)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M49 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M49
          topology: tests/interop/m49-evpn-preference-df.clab.yml
          script: tests/interop/scripts/test-m49-evpn-preference-df.sh

  m69:
    name: M69 — EVPN preference-DF election (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      - name: Run M69 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M69
          topology: tests/interop/m69-evpn-preference-df-frr.clab.yml
          script: tests/interop/scripts/test-m69-evpn-preference-df-frr.sh

  m70:
    name: M70 — ADR-0089 VLAN-aware bridge FDB attribution (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      # Cross-vendor proof for ADR-0089's v1 Linux topology: rustbgpd owns
      # one vlan_filtering=1 bridge with explicit bridge_vlan bindings, FRR
      # originates the same MAC from two traditional VNIs, and rustbgpd must
      # program/remove only the matching VLAN-scoped FDB row.
      - name: Run M70 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M70
          topology: tests/interop/m70-evpn-vlan-aware-bridge-frr.clab.yml
          script: tests/interop/scripts/test-m70-evpn-vlan-aware-bridge-frr.sh

  m65:
    name: M65 — ADR-0083 single-active failover blackout measurement (GoBGP 3.x)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      # The segment PEs run GoBGP (the failure injection needs the RFC 7432
      # §8.2 mass-withdraw wire shape — EAD-per-ES withdrawn with the MAC
      # routes retained — which neither rustbgpd nor FRR can originate; see
      # the topology header).
      - name: Build gobgp:interop image
        run: docker build -t gobgp:interop -f tests/interop/Dockerfile.gobgp tests/interop
      - name: Run M65 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M65
          topology: tests/interop/m65-evpn-single-active-failover.clab.yml
          script: tests/interop/scripts/test-m65-evpn-single-active-failover.sh

  m71:
    name: M71 — RFC 9136 §4.3 ESI overlay-index Type 5 single-active receive (GoBGP 3.x)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      # rustbgpd is the receive-side DUT; GoBGP is the route source. GoBGP
      # (not FRR) because the proof needs byte-exact, independent control over
      # the EAD-per-ES Single-Active vs All-Active ESI Label flag injected and
      # withdrawn separately from the Type 5 — FRR's EVPN multi-homing is
      # all-active only and cannot originate a single-active EAD-per-ES.
      - name: Build gobgp:interop image
        run: docker build -t gobgp:interop -f tests/interop/Dockerfile.gobgp tests/interop
      - name: Run M71 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M71
          topology: tests/interop/m71-evpn-esi-overlay-type5-receive-gobgp.clab.yml
          script: tests/interop/scripts/test-m71-evpn-esi-overlay-type5-receive-gobgp.sh

  m66:
    name: M66 — ADR-0084 ES drain service handover (rustbgpd ×3)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      # All three BGP speakers are rustbgpd — the origination-side
      # withdrawal stimulus M65 lacked is the ADR-0084 runtime ES
      # drain, so no GoBGP image is needed (see the topology header).
      - name: Run M66 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M66
          topology: tests/interop/m66-evpn-es-drain-handover.clab.yml
          script: tests/interop/scripts/test-m66-evpn-es-drain-handover.sh

  m67:
    name: M67 — ADR-0085 link-driven ES drain failover (rustbgpd ×3)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      # The M66 sibling with the production trigger: an attachment-
      # circuit carrier loss (ip link set down inside pe1) — not an
      # RPC — drives the drain, the RFC 7432 §8.2 withdrawal shape,
      # the DF handover, and the held-off recovery (see the topology
      # header). All three BGP speakers are rustbgpd.
      - name: Run M67 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M67
          topology: tests/interop/m67-evpn-link-drain-failover.clab.yml
          script: tests/interop/scripts/test-m67-evpn-link-drain-failover.sh

  m68:
    name: M68 — ADR-0087 GW-IP overlay-index Type 5 (FRR 10.3.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/setup-dataplane-host
      # Cross-vendor consume-side proof for native GW-IP overlay-index
      # Type 5 origination: FRR holds the Type 5 unresolved until the
      # companion Type 2 exists, then imports the prefix via the Gateway
      # Address using enable-resolve-overlay-index.
      - name: Run M68 (deploy + test + destroy with retry)
        uses: ./.github/actions/run-interop-test
        with:
          label: M68
          topology: tests/interop/m68-evpn-type5-gwip-overlay-index-frr.clab.yml
          script: tests/interop/scripts/test-m68-evpn-type5-gwip-overlay-index-frr.sh

  netns:
    name: Privileged netns selectors (fdb_nhg + fib_runtime + bfd_runtime + dataplane_vlan_fdb + svd_fdb_vni + l3_multipath)
    runs-on: ubuntu-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6

      # The Docker netns harness builds its own image and runs the privileged
      # tests inside `docker run --cap-add=NET_ADMIN --cap-add=SYS_ADMIN`, so the
      # netns never leaks to the host namespace. All selectors except
      # l3_multipath are vrf-free and need no host setup beyond Docker
      # (preinstalled); l3_multipath creates a `vrf` device, so the step below
      # loads the vrf module on the host kernel the containers share.
      - name: ADR-0059 FDB nexthop group netns tests
        run: bash crates/evpn-linux/tests/docker/run-netns-tests.sh fdb_nhg

      - name: ADR-0061 FIB runtime netns test
        run: bash crates/evpn-linux/tests/docker/run-netns-tests.sh fib_runtime

      - name: ADR-0067 BFD runtime netns test
        run: bash crates/evpn-linux/tests/docker/run-netns-tests.sh bfd_runtime

      - name: ADR-0089 VLAN-scoped FDB netns test
        run: bash crates/evpn-linux/tests/docker/run-netns-tests.sh dataplane_vlan_fdb

      - name: LAN-64 SVD collect-metadata FDB VNI netns test
        run: bash crates/evpn-linux/tests/docker/run-netns-tests.sh svd_fdb_vni

      # l3_multipath creates a `vrf` device; the slim hosted Azure kernel ships
      # vrf only via linux-modules-extra. Load it on the host so the --cap-add
      # netns containers can create vrf devices (mirrors setup-dataplane-host).
      - name: Load vrf kernel module (for l3_multipath)
        run: |
          if ! sudo modprobe vrf 2>/dev/null; then
            sudo apt-get install -y "linux-modules-extra-$(uname -r)" || \
              echo "::warning::linux-modules-extra-$(uname -r) unavailable; l3_multipath may fail"
            sudo modprobe vrf || echo "::warning::vrf module still not loadable"
          fi

      - name: LAN-70 L3VNI multipath and FDB-NHG netns proof
        run: bash crates/evpn-linux/tests/docker/run-netns-tests.sh l3_multipath
