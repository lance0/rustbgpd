# GENERATED by rs-config-render — do not edit; edits are overwritten on the
# next refresh. Regenerate from `arouteserver template-context` output.
# Route-server configuration. Context-shape fingerprint: 6cf0b6c6d60bb276.

[global]
asn = 65500
router_id = "192.0.2.1"
listen_port = 179
# RFC 8326: honor GRACEFUL_SHUTDOWN from members (local-pref 0 chain tail).
honor_graceful_shutdown = true

# Local management surface (`rbgp` over UDS, file-permission gated) —
# the same shape as examples/route-server/. Add prometheus_addr here to
# scrape metrics.
[global.telemetry]
log_format = "json"

[global.telemetry.grpc_uds]
path = "/var/lib/rustbgpd/grpc.sock"
principal = "operator"

[security.grpc]
enforcement = "tier"

[security.grpc.roles]
operator = "operator"

[rpki]
[[rpki.cache_servers]]
address = "127.0.0.1:3323"

[policy]
rpol_files = [
    "policy/rs-hygiene.rpol",
    "policy/client-as4242-1.rpol",
    "policy/client-as197000-1.rpol",
]

[[neighbors]]
address = "192.0.2.11"
remote_asn = 4242
description = "member-one"
families = ["ipv4_unicast"]
route_server_client = true
role = "route_server"
next_hop_ownership = "strict_peer"
per_client_best = true
max_prefixes_ipv4 = 5000
import_policy_chain = ["rs-hygiene", "client-as4242-1"]

[[neighbors]]
address = "2001:db8:0:1::22"
remote_asn = 197000
description = "member-two-v6"
families = ["ipv6_unicast"]
route_server_client = true
role = "route_server"
next_hop_ownership = "strict_peer"
ttl_security = true
per_client_best = true
max_prefixes_ipv6 = 1000
import_policy_chain = ["rs-hygiene", "client-as197000-1"]
