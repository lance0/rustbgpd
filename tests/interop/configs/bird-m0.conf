log stderr all;
router id 10.0.1.2;

protocol device {
  scan time 10;
}

protocol direct {
  ipv4;
  interface "eth1";
}

protocol bgp rustbgpd {
  local 10.0.1.2 as 65003;
  neighbor 10.0.1.1 as 65001;
  hold time 90;

  ipv4 {
    import all;
    export none;
  };
}
