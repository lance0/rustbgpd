[package]
publish = false
name = "rustbgpd-evpn-linux"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
keywords.workspace = true
categories.workspace = true
description = "Linux netlink dataplane reconciler for EVPN VTEP — observes bridge/VXLAN topology and programs remote-MAC FDB entries (RFC 7432 / RFC 8365)"

[dependencies]
rustbgpd-evpn.workspace = true
thiserror.workspace = true
tokio = { workspace = true, features = ["sync", "time", "macros", "rt"] }
tokio-util = { workspace = true, features = ["rt"] }
tracing.workspace = true

# Linux-only real netlink; cfg-gating preserves portable trait/test surfaces
# without promising non-Linux daemon support. The netlink crates form a coherent ecosystem
# released together; bump them as a set to avoid duplicate major-version families.
[target.'cfg(target_os = "linux")'.dependencies]
rtnetlink = "0.23"
netlink-packet-route = "0.33"
netlink-packet-core = "0.9"
netlink-sys = { version = "0.9", features = ["tokio_socket"] }
futures = "0.3"
libc.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["full", "test-util"] }
