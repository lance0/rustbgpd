include "conf.d/*.conf";
router id 192.0.2.10;

protocol bgp member {
  local as 64500;
  include "peer.conf";
  neighbor 192.0.2.1 as 64496;
  ipv4 {
    include "policy.conf";
    import all;
  };
}

protocol device {
  include "device.conf";
}
