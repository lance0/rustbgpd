# Enhanced Route Refresh v0.68.0 compact artifacts

`summary.json` retains the exact-release phase, correctness, actor-duration,
and memory observations. `provenance.env` identifies the run. This compact
copy omits raw logs and route snapshots.
